'use strict';

const box = document.querySelector('#box');
const buttons = document.querySelectorAll('button');
const circles = document.querySelectorAll('.circe');
const hearts = document.querySelectorAll('.heart');